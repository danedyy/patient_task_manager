name: patient_task_manager
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.12.2

dependencies:
  bloc_concurrency: ^0.3.0
  connectivity_plus: ^7.2.0
  drift: ^2.34.1
  drift_flutter: ^0.3.0
  equatable: ^2.1.0
  flutter:
    sdk: flutter
  flutter_bloc: ^9.1.1
  freezed_annotation: ^3.1.0
  rxdart: ^0.28.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  build_runner: ^2.15.0
  freezed:
  json_serializable: ^6.14.0
  drift_dev:
  bloc_test: ^10.0.0
  mocktail: ^1.0.5
  fake_async: ^1.3.3

dependency_overrides:
  sqlparser: '>=0.44.0 <0.44.6'

flutter:
  uses-material-design: true
